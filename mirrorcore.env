#!/bin/bash

# ☲ Properly source the env file
if [ -f "$HOME/mirrorcore/.env" ]; then
    source "$HOME/mirrorcore/.env"
    echo "🜂 MirrorCore environment loaded."
    echo "Root: $MIRRORCORE_HOME"
else
    echo "⚠️ .env file not found."
fi
